{
  "name": "@agent-pane/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle:bridge": "node ./scripts/bundle-bridge.mjs",
    "dev": "npm run bundle:bridge && tauri dev",
    "build": "npm run bundle:bridge && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "@tauri-apps/plugin-shell": "^2.2.1"
  }
}
